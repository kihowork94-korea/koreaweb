name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 * * 1'  # 매주 월요일 자정 (UTC)
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sf https://korea-web-two.vercel.app/api/lab/lab_info
